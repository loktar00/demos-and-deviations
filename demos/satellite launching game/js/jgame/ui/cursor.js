/*** Cursor is just a sprite that matches its position with the mouse coords. **/(function(){	var utilities = new Utilities();		function Cursor(options){		Sprite.call(this, options);	}		Cursor.prototype = new Sprite();	this.Cursor = Cursor;			Cursor.prototype.update = function(deltaTime){		Sprite.prototype.update();		if(Game.mouseX < Game.width-this.width){			this.pos.x = Game.mouseX;		}				if(Game.mouseY < Game.height-this.height){			this.pos.y = Game.mouseY;		}	}})();